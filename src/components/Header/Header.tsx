import { Box, Button } from "@mui/material"
import styles from "./header.module.scss"

export const Header=()=>{
    return(
        <Box className={styles.header}>
        <p>Header</p>
        </Box>
    )
}